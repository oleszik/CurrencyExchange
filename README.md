# CurrencyExchange
A console-based currency converter that uses real-time exchange rates from the FloatRates API. It supports converting between currencies and uses caching to store previously fetched rates for faster access and better performance during repeated conversions.
